export interface ShadowImageDataType {
  imageURL: string;
  shadowPosition: "left" | "right" | "";
  shadowDimension: "small" | "medium" | "large" | "";
}

export interface ImageRendererDataTypes {
  imgURL: string;
  alignImage: string;
}
